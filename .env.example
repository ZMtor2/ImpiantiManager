# Database
DATABASE_URL="postgresql://impianti:impianti_secret@localhost:5432/impianti_manager"

# Auth
AUTH_SECRET="cambia-questo-valore-in-produzione-32-chars-min"
NEXTAUTH_URL="http://localhost:3000"

# Storage (MinIO o S3-compatibile)
STORAGE_ENDPOINT="http://localhost:9000"
STORAGE_ACCESS_KEY="minioadmin"
STORAGE_SECRET_KEY="minioadmin"
STORAGE_BUCKET="impianti-docs"

# Email (per inviti utente)
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="noreply@impiantimanager.it"
